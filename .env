# .env 文件示例，用于配置 Node.js 认证网关应用

# 认证网关对外暴露的端口
# 默认值: 8100
AUTH_GATE_PUBLIC_PORT=8100

# 内部主应用程序 (server.js) 监听的端口
# 默认值: 3000
AUTH_GATE_APP_INTERNAL_PORT=3000

# 存储数据文件的目录路径
# 例如: master_auth_config.enc, user_credentials.enc, encryption.secret.key
# 默认值: 脚本所在的目录 (__dirname)
# 您可以指定一个绝对路径，例如: /var/data/auth_gate_data
# 或者一个相对于脚本的路径，例如: ./data (确保这个目录存在且应用有读写权限)
AUTH_GATE_DATA_DIR=./auth_data
