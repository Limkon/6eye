{
  "name": "6eye-worker-refactored",
  "version": "2.0.0",
  "main": "dist/_worker.js",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "cookie": "^0.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "wrangler": "^3.91.0"
  }
}
